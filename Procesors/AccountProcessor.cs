using budget_manager_mapper.Models;

namespace budget_manager_mapper.Procesors
{
    // public class AccountProcessor : AbstractProcessor, IProcessor
    // {
    //     public void processFile(string fileName, string outputFilename)
    //     {
    //         var accounts = GetJsonFromFile<AccountIn[]>(fileName);
    //         var outAccounts = _mapper.Map<AccountOut[]>(accounts);

    //         this.SaveJson(outAccounts, outputFilename);
    //     }
    // }
}